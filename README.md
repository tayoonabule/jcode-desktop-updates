# JCode Desktop Updates

This public repository contains only signed release artifacts for JCode Desktop automatic updates. The application source, build configuration, and CI implementation remain private.

Do not download an update unless it is offered by JCode Desktop. Sparkle verifies the signed appcast and update archive before installation.
